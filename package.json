{
  "name": "bmkg-api",
  "version": "1.0.0",
  "description": "BMKG API - Indonesian Weather & Earthquake REST API",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./landing/input.css -o ./landing/output.css --minify",
    "watch:css": "tailwindcss -i ./landing/input.css -o ./landing/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
