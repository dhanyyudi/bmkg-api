# Docker Compose override for testing
# Usage: docker-compose -f docker-compose.yml -f docker-compose.test.yml up -d

version: '3.8'

services:
  bmkg-api-server:
    ports:
      - "8099:8099"
    # Don't expose ports in production, only for testing
